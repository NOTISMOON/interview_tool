# 待开发静态页面任务清单

> 更新日期：2026-08-10
> 项目：AI 面试教练（interview_tool）

---

## 一、任务概览

当前项目共有 **21 个已完成页面**，**0 个待开发静态页面**，**0 个次要功能**（全部已实现）。

### 已完成页面（21 个）

| 路由 | 页面 | 状态 |
|------|------|------|
| `/` | 官网首页 LandingPage | ✅ 完成 |
| `/login` | 登录页 LoginPage | ✅ 完成 |
| `/register` | 注册页 RegisterPage | ✅ 完成 |
| `/privacy` | 隐私政策 PrivacyPage | ✅ 完成 |
| `*` | 404 页面 NotFoundPage | ✅ 完成 |
| `/dashboard` | 工作台 DashboardHome | ✅ 完成 |
| `/dashboard/interview` | 面试上传 InterviewPage | ✅ 完成 |
| `/dashboard/interview/session/:id` | 面试答题 InterviewSession | ✅ 完成 |
| `/dashboard/history` | 面试记录 HistoryPage | ✅ 完成 |
| `/dashboard/report/:id` | 面试报告 ReportPage | ✅ 完成 |
| `/dashboard/feed` | 关注动态 FeedPage | ✅ 完成 |
| `/dashboard/community` | 社区列表 CommunityPage | ✅ 完成 |
| `/dashboard/community/post/:id` | 帖子详情 PostDetailPage | ✅ 完成 |
| `/dashboard/messages` | 消息中心 MessagesPage | ✅ 完成 |
| `/dashboard/profile` | 个人设置 ProfilePage | ✅ 完成 |
| `/dashboard/following` | 关注列表 FollowingPage | ✅ 完成 |
| `/dashboard/followers` | 粉丝列表 FollowersPage | ✅ 完成 |
| `/dashboard/favorites` | 我的收藏 FavoritesPage | ✅ 完成 |
| `/dashboard/settings` | 设置 SettingsPage | ✅ 完成 |
| `/dashboard/help` | 帮助与反馈 HelpPage | ✅ 完成 |

---

## 二、已完成的页面详情（2026-08-10 更新）

### 1. 404 页面 ✅

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **路由** | `*`（全局兜底） |
| **文件路径** | `src/pages/NotFound/index.tsx` |
| **功能** | 大号 404 状态码 + 友好提示文案 +「返回首页」/「返回工作台」按钮（根据登录状态） +「返回上一步」按钮 |
| **设计** | 墨黑(#0D1117) 背景 + 焰橙(#FF6B35) 渐变大号 404 |

### 2. 社区帖子详情页 ✅

| 字段 | 内容 |
|------|------|
| **优先级** | P0 |
| **路由** | `/dashboard/community/post/:id` |
| **文件路径** | `src/pages/Dashboard/PostDetail.tsx` |
| **功能** | 帖子完整内容、作者信息、标签、点赞/评论/浏览数；评论区展示（mock 数据）；底部快捷操作（点赞、收藏、分享、评论输入）；帖子不存在时展示空状态 |
| **关联修改** | `Community.tsx` 帖子点击 → `navigate`；`Feed.tsx` 帖子点击 → `navigate` |

### 3. 关注列表页 ✅

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **路由** | `/dashboard/following` |
| **文件路径** | `src/pages/Dashboard/Following.tsx` |
| **功能** | 展示关注用户列表（头像、昵称、简介）；关注/取消关注按钮切换；空状态展示；返回按钮回到个人设置 |
| **关联修改** | `Profile.tsx` 菜单入口和统计卡片点击 → 跳转本页 |
| **数据** | 新增 `UserBrief` 类型（`types/user.ts`）+ `mockFollowingList`（`mocks/data.ts`） |

### 4. 粉丝列表页 ✅

| 字段 | 内容 |
|------|------|
| **优先级** | P1 |
| **路由** | `/dashboard/followers` |
| **文件路径** | `src/pages/Dashboard/Followers.tsx` |
| **功能** | 展示粉丝列表（头像、昵称、简介、关注标签）；回关/互相关注/已关注按钮状态切换；空状态展示；返回按钮回到个人设置 |
| **关联修改** | `Profile.tsx` 菜单入口和统计卡片点击 → 跳转本页 |
| **数据** | 复用 `UserBrief` 类型 + `mockFollowersList`（`mocks/data.ts`） |

---

## 三、第三轮 P2 功能（2026-08-10 完成）

### 5. 编辑资料 ✅

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **实现方式** | Profile.tsx 内 Modal 弹窗 |
| **功能** | 修改昵称（Input + 字数统计）；更换头像（点击上传图片，支持 FileReader 预览）；保存后通过 `updateUser` 更新 Store |
| **关联修改** | Store 新增 `updateUser` action；`Profile.tsx` 编辑按钮 → 打开 Modal |

### 6. 我的收藏 ✅

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **路由** | `/dashboard/favorites` |
| **文件路径** | `src/pages/Dashboard/Favorites.tsx` |
| **功能** | 展示收藏帖子列表；点击跳转帖子详情；取消收藏（confirm 确认）；空状态展示 |

### 7. 设置 ✅

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **路由** | `/dashboard/settings` |
| **文件路径** | `src/pages/Dashboard/Settings.tsx` |
| **功能** | 通知设置（邮件 / 推送 / 声音 Switch 开关）；隐私设置（公开主页 / 修改密码）；通用设置（语言 / 清除缓存）；注销账号按钮 |

### 8. 隐私政策 ✅

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **路由** | `/privacy`（公开页面） |
| **文件路径** | `src/pages/Privacy/index.tsx` |
| **功能** | 9 个章节的隐私政策内容（信息收集/使用/存储/共享/Cookie/安全/权利/更新/联系）；独立公开页面，不需登录 |

### 9. 帮助与反馈 ✅

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **路由** | `/dashboard/help` |
| **文件路径** | `src/pages/Dashboard/Help.tsx` |
| **功能** | 顶部 Banner（在线客服 / 邮箱入口）；8 条 FAQ 折叠面板；底部提交反馈入口 |

### 10. 发帖 ✅

| 字段 | 内容 |
|------|------|
| **优先级** | P2 |
| **实现方式** | Community.tsx 内 Modal 弹窗 |
| **功能** | 标题输入（必填 + 字数统计）；内容输入（必填 + 字数统计）；标签选择（输入匹配 + 点击添加，最多 5 个）；**图片上传**（最多 4 张，FileReader 转 base64 预览，支持删除）；表单校验 + 发布成功提示 |

---

## 四、当前路由配置（App.tsx）

```
/                              → LandingPage          (公开)
/login                         → LoginPage            (公开)
/register                      → RegisterPage         (公开)
/privacy                       → PrivacyPage          (公开)
*                              → NotFoundPage          (公开，全局兜底)
/dashboard                     → DashboardLayout      (需登录)
  /dashboard                   → DashboardHome
  /dashboard/interview         → InterviewPage
  /dashboard/interview/session/:id → InterviewSession
  /dashboard/history           → HistoryPage
  /dashboard/report/:id        → ReportPage
  /dashboard/feed              → FeedPage
  /dashboard/community         → CommunityPage
  /dashboard/community/post/:id → PostDetailPage
  /dashboard/messages          → MessagesPage
  /dashboard/profile           → ProfilePage
  /dashboard/following         → FollowingPage
  /dashboard/followers         → FollowersPage
  /dashboard/favorites         → FavoritesPage
  /dashboard/settings          → SettingsPage
  /dashboard/help              → HelpPage
```

---

## 五、开发顺序总结

```
第一轮（P0）    第二轮（P1）    第三轮（P2）
─────────      ─────────      ─────────
✅ 404 页面    ✅ 关注列表页   ✅ 编辑资料
✅ 帖子详情页   ✅ 粉丝列表页   ✅ 我的收藏
                              ✅ 设置
                              ✅ 隐私政策
                              ✅ 帮助与反馈
                              ✅ 发帖（含图片上传）
```

**全部 21 个页面/功能已开发完成。**

---

## 六、设计规范（统一引用）

所有新页面遵循以下设计规范：

- **色彩**：墨黑 `#0D1117` / 焰橙 `#FF6B35` / 灰白 `#F6F8FA` / 岩灰 `#5F6B7A` / 线灰 `#E1E4E8`
- **圆角**：卡片 `14px` / 按钮 `10px` / 小元素 `6px`
- **卡片**：白色背景 + 1px 边框 `#E1E4E8` + `14px` 圆角
- **按钮**：`btn-flame` 焰橙填充 / `btn-ghost` 透明边框
- **标签**：`tag` / `tag-flame`
- **布局**：后台内容区 `max-w-[700px]` 或全宽，`p-6` 内边距
- **字体**：标题 `font-extrabold` / 正文 `text-sm font-medium` / 辅助 `text-xs text-[#8B949E]`